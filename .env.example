# Required server-only Open WebUI configuration.
# Your Open WebUI server URL, e.g. https://openwebui.example.com
OPENWEBUI_BASE_URL=
OPENWEBUI_API_KEY=
OPENWEBUI_MODEL=qwen3.5:122b-a10b

# Public app URL for Docker deployments. Set this to your public app URL.
ORIGIN=http://localhost:3000

# Ordered map-data endpoints. Each is tried once; failing hosts cool down for 30 s.
OVERPASS_URLS=https://overpass-api.de/api/interpreter,https://overpass.private.coffee/api/interpreter
